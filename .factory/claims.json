[
  {
    "id": "csv-import",
    "claim": "Imports completed-work and invoice CSV exports",
    "where": "landing workspace, README",
    "test": "npm test -- --grep @claim:csv-import",
    "sandbox": "fresh browser context; upload two in-memory CSV fixtures and assert parsed rows, amount, and suggested invoice"
  },
  {
    "id": "multi-source-import",
    "claim": "Adds completed-work exports from multiple tools, labels each source, skips exact duplicate rows, and replaces only a confirmed source",
    "where": "landing first screen, workspace source list, README",
    "test": "npm test -- --grep @claim:multi-source-import",
    "sandbox": "fresh browser context; add task-tool and time-tool fixtures, assert both labelled rows and their combined total, skip an exact duplicate, then confirm replacing one source while the other remains"
  },
  {
    "id": "demo-sample-ready",
    "claim": "One click opens an isolated sample with six work rows, two suggestions, four review rows, a $5,840 total, a working reset, and an actionable row in the first phone viewport",
    "where": "landing first screen, demo first screen",
    "test": "npm test -- --grep @claim:demo-sample-ready",
    "sandbox": "fresh 390 by 844 browser context; click the landing demo action once, assert the sample composition and banner, measure the actionable row inside the viewport, change the total, then reset to the original sample"
  },
  {
    "id": "header-mapping",
    "claim": "CSV headers do not need to match the app's field names because the user can map them before import",
    "where": "README, CSV mapping panel",
    "test": "npm test -- --grep @claim:header-mapping",
    "sandbox": "fresh browser context; upload completed-work and invoice CSV fixtures with unrelated header names, map their required fields, and assert the row, value, and suggested invoice import"
  },
  {
    "id": "queue-filter",
    "claim": "Already billed and unfinished completed-work rows are excluded from the unbilled-work list",
    "where": "demo list summary, README",
    "test": "npm test -- --grep @claim:queue-filter",
    "sandbox": "fresh browser context; import completed, already-billed, and unfinished work rows and assert only the completed unbilled row and its value appear"
  },
  {
    "id": "missing-status",
    "claim": "A completed-work CSV row with no status column is treated as completed",
    "where": "README",
    "test": "npm test -- --grep @claim:missing-status",
    "sandbox": "fresh browser context; import work without a status column and assert it appears in the unbilled-work list"
  },
  {
    "id": "validated-import",
    "claim": "Blank required cells, invalid amounts, and non-finite calculations are rejected without replacing saved data",
    "where": "README, CSV mapping errors",
    "test": "npm test -- --grep @claim:validated-import",
    "sandbox": "fresh browser context; save valid work, attempt blank, non-numeric, and finite-but-overflowing rows plus a blank invoice row, assert row-number errors and unchanged data, then recover with a valid file"
  },
  {
    "id": "review-matches",
    "claim": "Invoice matching suggestions stay under the user's control",
    "where": "landing, demo, README",
    "test": "npm test -- --grep @claim:review-matches",
    "sandbox": "fresh demo; link one suggestion, reload, then keyboard-unlink it and assert the row and full queue total return"
  },
  {
    "id": "work-replacement",
    "claim": "Replacing one completed-work source keeps its unchanged review decisions and returns its changed rows to the unbilled-work list",
    "where": "README, work replacement status",
    "test": "npm test -- --grep @claim:work-replacement",
    "sandbox": "fresh browser context; review two rows from one source, confirm replacing only that source with one identical and one changed row, then assert the unchanged decision remains and the changed row returns for review"
  },
  {
    "id": "invoice-replacement",
    "claim": "Replacing invoice data clears links to invoices that are no longer present",
    "where": "README, invoice replacement status",
    "test": "npm test -- --grep @claim:invoice-replacement",
    "sandbox": "fresh demo; link the first suggestion, retain it after the same invoice moves rows, then replace with an unrelated CSV and assert the stale link clears and the full queue returns"
  },
  {
    "id": "csv-export",
    "claim": "Exports the unbilled-work list as an invoice-draft checklist CSV",
    "where": "workspace toolbar, README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "fresh demo; inspect the downloaded CSV header and assert one row per queue item"
  },
  {
    "id": "local-only",
    "claim": "CSV rows and demo review actions stay on the device",
    "where": "landing facts, privacy, README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context; import a private row, review demo work, and assert that no request leaves the app origin"
  },
  {
    "id": "network-boundary",
    "claim": "CSV and demo actions make no off-origin requests; buying or verifying a license contacts api.sociobot.in",
    "where": "privacy notice, README",
    "test": "npm test -- --grep @claim:network-boundary",
    "sandbox": "fresh browser context; log requests through import and demo actions, then intercept checkout and license verification and assert only those explicit actions contact api.sociobot.in"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load the normal landing route, wait for its service worker, disable the HTTP cache and network, reload, and assert the landing workspace"
  },
  {
    "id": "manifest-mime",
    "claim": "The web app manifest is served with a standards-compatible JSON content type",
    "where": "PWA installation metadata",
    "test": "npm test -- --grep @claim:manifest-mime",
    "sandbox": "production build preview; request manifest.webmanifest, assert a manifest JSON content type, and assert the static deployment rewrites it to an identical JSON asset"
  },
  {
    "id": "runtime-asset-cache",
    "claim": "The service worker stores fetched same-origin assets for offline reuse",
    "where": "service worker behavior",
    "test": "npm test -- --grep @claim:runtime-asset-cache",
    "sandbox": "fresh browser context on a unique origin; fetch the shipped social-preview asset and assert its same-origin path enters the service-worker cache"
  },
  {
    "id": "local-persistence",
    "claim": "Real workspace data stays in IndexedDB across a reload",
    "where": "README, workspace",
    "test": "npm test -- --grep @claim:local-persistence",
    "sandbox": "fresh browser context; import one real work row, reload, and assert its description and amount remain"
  },
  {
    "id": "workspace-backup",
    "claim": "Exports and imports a complete workspace JSON backup",
    "where": "workspace toolbar, README",
    "test": "npm test -- --grep @claim:workspace-backup",
    "sandbox": "fresh demo; download the sample workspace, enter the empty real workspace, restore the file, and assert its total"
  },
  {
    "id": "paid-license",
    "claim": "$19 one-time review history uses Sociobot checkout and license verification",
    "where": "landing paid section, README, terms",
    "test": "npm test -- --grep @claim:paid-license",
    "sandbox": "fresh browser context; assert checkout URL, mock a valid Sociobot verification response, and assert paid state"
  },
  {
    "id": "snapshot-history",
    "claim": "A paid license adds review history with named weekly list totals on this device",
    "where": "landing paid section, README",
    "test": "npm test -- --grep @claim:snapshot-history",
    "sandbox": "fresh browser context; mock a valid license, save two named demo reviews with different queue states, and assert both counts and values are visible"
  },
  {
    "id": "hours-times-rate",
    "claim": "When amount is missing, imports calculate it from hours multiplied by rate",
    "where": "landing workspace, README",
    "test": "npm test -- --grep @claim:hours-times-rate",
    "sandbox": "fresh browser context; import a completed row with 2 hours and a $75 rate, then assert its $150 queue value"
  },
  {
    "id": "invoice-date-guard",
    "claim": "Imports accept real YYYY-MM-DD and M/D/YYYY calendar dates, and invoices dated before the work are not suggested",
    "where": "README, CSV mapping panel",
    "test": "npm test -- --grep @claim:invoice-date-guard",
    "sandbox": "fresh browser context; reproduce 10/1/2026 work versus 9/1/2026 invoice, assert no suggestion, accept a cross-year later invoice, then reject malformed and impossible dates without replacing data"
  },
  {
    "id": "match-normalization",
    "claim": "Suggestions ignore capitalization, punctuation, and spacing in client and project names, and do not suggest a different project",
    "where": "README",
    "test": "npm test -- --grep @claim:match-normalization",
    "sandbox": "fresh browser context; import a client wording variation with a matching project and a same-client different-project near miss, then assert only the matching invoice is suggested"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo data uses separate browser storage and is discarded when you start for real",
    "where": "demo banner, privacy notice, README, demo contract",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; retain a real row, review demo data, leave demo, then assert the real row remains and demo keys are gone"
  },
  {
    "id": "demo-session-removal",
    "claim": "Demo review data is removed when the browser session ends",
    "where": "privacy notice",
    "test": "npm test -- --grep @claim:demo-session-removal",
    "sandbox": "review a demo row in one fresh browser context, close the context, open a second context, and assert the sample has no retained decision"
  },
  {
    "id": "clear-workspace",
    "claim": "Clear imported data removes the current workspace",
    "where": "privacy notice, README, workspace control",
    "test": "npm test -- --grep @claim:clear-workspace",
    "sandbox": "fresh real workspace; import one row, confirm Clear imported data, reload, and assert the workspace remains empty"
  },
  {
    "id": "license-storage",
    "claim": "License verification stores only the license token and its latest verification result",
    "where": "privacy notice",
    "test": "npm test -- --grep @claim:license-storage",
    "sandbox": "fresh browser context; mock one invalid Sociobot verification, then assert localStorage contains exactly the token and verdict keys with their expected values"
  },
  {
    "id": "free-core",
    "claim": "Imports, review, and checklist exports stay free",
    "where": "landing paid section, README",
    "test": "npm test -- --grep @claim:free-core",
    "sandbox": "fresh demo without a license; review a suggestion and download the checklist while review history remains marked paid"
  },
  {
    "id": "billing-boundary",
    "claim": "Review history costs $19 once through Sociobot checkout; no payment form is embedded in the app",
    "where": "landing paid section, README, terms",
    "test": "npm test -- --grep @claim:billing-boundary",
    "sandbox": "fresh browser context; assert the one-time copy, Sociobot checkout URL, and absence of payment iframes or card fields"
  },
  {
    "id": "scope-boundaries",
    "claim": "The sweep reviews exported records; it does not send invoices, track time, calculate tax, or change source files",
    "where": "landing boundary section, README",
    "test": "npm test -- --grep @claim:scope-boundaries",
    "sandbox": "temporary source fixtures; hash the work file before and after import, match review, checklist export, and workspace export; assert no off-origin or send request and inspect every interactive action for invoice-sending, time-tracking, or tax behavior"
  },
  {
    "id": "art-disclosure",
    "claim": "The footer discloses that the product artwork was generated for this product",
    "where": "footer, design record",
    "test": "npm test -- --grep @claim:art-disclosure",
    "sandbox": "fresh browser context; open the landing page and assert the footer artwork disclosure"
  }
]
